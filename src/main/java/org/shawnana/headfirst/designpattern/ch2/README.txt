观察者模式：在对象之间定义一对多的依赖，这样一来，当一个对象状态发生改变时，依赖于它的对象都会收到通知，并自动更新。

在示例代码中，分别使用了jdk提供的Observer接口和Observable类，可自己实现的Subject接口和Observer接口。
其中，自己实现的观察者模式代码只支持“推”的方式，而jdk提供的实现支持“推”和“拉”。
两者之间的相同点是：触发都需要由被观察者发起。不同点是：“拉”的方式有利于观察者自己选择获取的数据，而“推”的方式往往
是连观察者不关注的数据也一起被推送过来。