命令模式：将“请求”封装成对象，以便使用不同的请求、队列或者日志来参数化其他对象。命令模式也支持可撤销的操作。

命令模式通过将“实际动作的执行者”与“需要执行的动作”封装成一系列命令对象，实现调用者与执行者之间的解耦。调用者并不了解其所持有的Command的具体
类型，只需要调用Command的execute()方法即可。个人认为JDK中的Thread类与Runnable接口就是通过命令模式实现。

通过宏命令（MacroCommand），可以将多种命令组合成一个复合命令。